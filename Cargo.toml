[package]
name = "rustpad"
version = "0.1.0"
edition = "2024"

[dependencies]
chrono = { version = "0.4", default-features = false, features = ["clock", "std"] }
druid = { version = "0.8", features = ["im"] }
thiserror = "1"
